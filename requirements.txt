pandas==2.0.3
numpy==2.1.1        # (assuming this is still the latest stable version)
dash==2.18.2        # (assuming this remains current)
dash-bootstrap-components==1.6.0
plotly==5.24.1
# Optional for deployment:
gunicorn==21.0.0    # or latest available
